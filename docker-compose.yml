version: '3.8'

services:
  gpx-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./app.py:/app/app.py
      - ./static:/app/static
    restart: unless-stopped
